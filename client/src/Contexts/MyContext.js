import { createContext } from "react";
// a context provider for a teacher
export const MyContext = createContext("")

// a context provider for a classe 
export const ClasseContext = createContext("")

// a context provider for
export const AffectationContext = createContext("")

// a context provider for isClasse or isAffectation
export const ActeurContext = createContext("")


