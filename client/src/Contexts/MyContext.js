import { createContext } from "react";
// a context provider for a teacher
export const MyContext = createContext("")

// a context provider for an affectation 
export const MyAffectation = createContext("")